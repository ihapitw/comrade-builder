const { getSourcePath } = require('./source-path')
const { formatName } = require('./format-name')
const { createTemplate } = require('./create-template')
const log = require('./log')

module.exports = { getSourcePath, formatName, log, createTemplate }
